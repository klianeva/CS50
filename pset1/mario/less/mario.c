#include <cs50.h>
# include <stdio.h>

int main (void)
{
  int n;
    do
    {
        n = get_int("Positive number less than 23: ");
    }
    while (n <= 0 || n > 23);

  for (int i = 4; i <= 4; i++)

   printf(" \n",i);
}